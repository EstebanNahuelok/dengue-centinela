node_modules/
dist/
build/
# Secretos. El patron .env.* cubre CUALQUIER variante (.env.production,
# .env.neon, etc): mas seguro listar la excepcion que enumerar los casos.
# El .env.example es lo unico que sube, y va con placeholders vacios.
.env
.env.*
!.env.example
# Ojo: este se llama SIN punto y habia quedado rastreado por error. Los
# patrones de arriba no lo cubrian.
env.local
*.env.local
*.log
.DS_Store
Thumbs.db

# Claude Code: subagentes y config local, no subir al repo del equipo
.claude/agents/*.md
CLAUDE.md

# Prisma
backend/prisma/dev.db
backend/prisma/migrations/**/migration.sql.bak

# Vite
frontend/.vite/
