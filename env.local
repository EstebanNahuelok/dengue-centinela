# Copiá a .env.local (Vite lo lee automaticamente y no lo pisa git).
VITE_API_URL=http://localhost:3001

# true = usa src/mocks/mockZonas.js en vez de pegarle al backend real.
# Dejalo en true mientras el backend no esta listo, despues pasalo a false.
VITE_USE_MOCK=true
